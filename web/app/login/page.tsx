"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { nhost } from "@/lib/nhost";
export default function LoginPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[mode,setMode]=useState<"in"|"up">("in");const[error,setError]=useState<string|null>(null);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError(null);const result=mode==="in"?await nhost.auth.signIn({email,password}):await nhost.auth.signUp({email,password});if(result.error){setError(result.error.message);return;}router.replace("/workflows");}return <div style={{maxWidth:360,margin:"80px auto",padding:24}}><h1 style={{fontSize:20}}>AI Workflow Builder</h1><form onSubmit={submit} style={{display:"flex",flexDirection:"column",gap:10,marginTop:16}}><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/><input placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/><button type="submit">{mode==="in"?"Sign in":"Sign up"}</button>{error&&<p style={{color:"#f66"}}>{error}</p>}</form><button style={{marginTop:12,background:"none",border:"none",color:"#8ab4f8",cursor:"pointer"}} onClick={()=>setMode(mode==="in"?"up":"in")}>{mode==="in"?"Need an account? Sign up":"Have an account? Sign in"}</button></div>;}
