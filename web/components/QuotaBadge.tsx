export function QuotaBadge({used,limit}:{used:number;limit:number}){const pct=limit>0?Math.min(100,Math.round((used/limit)*100)):0;const color=pct>=100?"#f66":pct>=80?"#f9a825":"#4caf50";return <div style={{display:"flex",alignItems:"center",gap:8,fontSize:13}}><span>Usage: {used} / {limit}</span><div style={{width:80,height:6,background:"#222634",borderRadius:4,overflow:"hidden"}}><div style={{width:`${pct}%`,height:"100%",background:color}}/></div></div>;}
